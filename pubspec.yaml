name: ml_bonus
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  image_picker: ^1.0.7
  image: ^4.1.3
  tflite_flutter: ^0.10.4
  google_fonts: ^6.1.0
  fluttertoast: ^8.2.4
  flutter_drawing_board: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/model/arabic_digit_ann.tflite
